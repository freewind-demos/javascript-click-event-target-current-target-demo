JavaScript Click Event "target" and "currentTarget" Demo
=========================================================

- `currentTarget`: 设置listener的那个DOM
- `target`: 事件发生的最小的DOM

Open `index.html` in your browser.
